apiVersion: 1

# Configured to add to existing Grafana server running on host machine.
datasources:
- name: Prometheus-op-stack-docker
  type: prometheus
  access: proxy
  url: http://localhost:9102
  editable: true